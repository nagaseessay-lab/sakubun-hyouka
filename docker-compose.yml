version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: essay_eval
      POSTGRES_USER: app
      POSTGRES_PASSWORD: devpassword
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
